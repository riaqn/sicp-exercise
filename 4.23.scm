;the version of Alyssa does not analyze the sequence itself. That
;means, every time the sequence is called, a loop on the sequence
                                        ;will be performed.
;The version in the text do more work, it has generate a procedure
;which contains all the expression in the sequence.
