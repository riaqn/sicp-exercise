;Yes, it should be more efficient, as long as the (sqrt) operation is
                                        ;not too slow
;This procedure is efficient because the redundant calculation of (+
;(* i i) (* j j)) is skipped.
